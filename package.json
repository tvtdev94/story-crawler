{
  "name": "story-crawler",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.26.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r --parallel --stream dev",
    "dev:web": "pnpm --filter @story-crawler/web dev",
    "dev:worker": "pnpm --filter @story-crawler/worker dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @story-crawler/db generate",
    "db:migrate": "pnpm --filter @story-crawler/db migrate",
    "db:migrate:deploy": "pnpm --filter @story-crawler/db migrate:deploy",
    "db:reset": "pnpm --filter @story-crawler/db reset",
    "db:seed": "pnpm --filter @story-crawler/db seed",
    "db:studio": "pnpm --filter @story-crawler/db studio",
    "compose:up": "docker compose -f docker/docker-compose.yml up -d",
    "compose:down": "docker compose -f docker/docker-compose.yml down",
    "compose:logs": "docker compose -f docker/docker-compose.yml logs -f"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
